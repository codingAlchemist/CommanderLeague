{
  "name": "commander-sign-up-sheet",
  "version": "0.0.0",
  "scripts": {
    "ng": "mkdir -p .angular-home .angular-config && CI=true HOME=\"$PWD/.angular-home\" XDG_CONFIG_HOME=\"$PWD/.angular-config\" npx ng",
    "start": "mkdir -p .angular-home .angular-config && CI=true HOME=\"$PWD/.angular-home\" XDG_CONFIG_HOME=\"$PWD/.angular-config\" npx ng serve --host 0.0.0.0",
    "build": "mkdir -p .angular-home .angular-config && CI=true HOME=\"$PWD/.angular-home\" XDG_CONFIG_HOME=\"$PWD/.angular-config\" npx ng build",
    "watch": "mkdir -p .angular-home .angular-config && CI=true HOME=\"$PWD/.angular-home\" XDG_CONFIG_HOME=\"$PWD/.angular-config\" npx ng build --watch --configuration development",
    "test": "mkdir -p .angular-home .angular-config && CI=true HOME=\"$PWD/.angular-home\" XDG_CONFIG_HOME=\"$PWD/.angular-config\" npx ng test"
  },
  "prettier": {
    "printWidth": 100,
    "singleQuote": true,
    "overrides": [
      {
        "files": "*.html",
        "options": {
          "parser": "angular"
        }
      }
    ]
  },
  "private": true,
  "packageManager": "npm@11.8.0",
  "dependencies": {
    "@angular/cdk": "~21.1.2",
    "@angular/common": "^21.1.0",
    "@angular/compiler": "^21.1.0",
    "@angular/core": "^21.1.0",
    "@angular/forms": "^21.1.0",
    "@angular/material": "~21.1.2",
    "@angular/platform-browser": "^21.1.0",
    "@angular/router": "^21.1.0",
    "rxjs": "~7.8.0",
    "scryfall-api": "^4.0.5",
    "tslib": "^2.3.0"
  },
  "devDependencies": {
    "@angular/build": "^21.1.2",
    "@angular/cli": "^21.1.2",
    "@angular/compiler-cli": "^21.1.0",
    "@angular/material": "^21.1.2",
    "jsdom": "^27.1.0",
    "typescript": "~5.9.2",
    "vitest": "^4.0.8"
  }
}
